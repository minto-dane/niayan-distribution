# 遅延トリガー参照状態の検証

対象source subjectは
`30d4b667646dad5a8732fec8a78c109b634d7e319c08680a67834d5b832938d2`。
結果は[report.json](report.json)、固定コンテナに渡した入力は[tested-inputs.json](tested-inputs.json)。
全入力hashと最終checkoutを照合している。

固定Debian開発コンテナの配布工具158試験が省略なしで成功した。
新しい参照状態13試験と従来のトリガー宣言・配送11試験を含む。
原本の発火先計算から未処理・待機へ接続し、旧新版の発火を同じ処理batchへまとめた。
同名の再発火、noawait追加、別受信者への待機、待機解除の連鎖、循環待機の保持を確認した。
処理中checkpointの再読は実行を開始せず、古いattemptの成功観測を拒否した。
内容hash・scope・正規形式・revision・資源上限も検査した。

ソース統合検査24工程が成功し、前後のsubjectは一致した。
全7コンポーネントの既存proof入力集合とhashは変わらず、同じ証明は再実行していない。
Python参照状態を形式証明済みとは扱わない。
重い試験は一つずつ、メモリ3 GiB・swap 0・CPU 1コア分・128プロセスの制限下で実行した。
固定コンテナはUID 1000・ネットワークなしで実行した。

この検証は参照遷移と状態codecの検査である。成功観測の認証、handler実行、
native CAS/WALへの接続、完全なpackage lifecycle、実電源断を検査したものではない。
モデルは導入済みDBを作らず、実行許可も返さない。
稼働PM、実boot切替、全DEB効果、完全置換ISO、全言語翻訳と応答の完全互換性は未完である。
