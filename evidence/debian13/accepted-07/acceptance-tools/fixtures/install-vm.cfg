# VM TEST ONLY. Not copied into production/live-build configuration.
# test-install.py creates the only attached disk and sets its serial explicitly.
d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select us
d-i netcfg/enable boolean false
d-i netcfg/get_hostname string niaos-test
d-i netcfg/get_domain string
d-i passwd/root-login boolean false
d-i passwd/user-fullname string NiaOS VM test
d-i passwd/username string niatest
d-i passwd/user-password password niaos-test-only
d-i passwd/user-password-again password niaos-test-only
d-i clock-setup/utc boolean true
d-i clock-setup/ntp boolean false
d-i time/zone string UTC
d-i partman/early_command string test "$(cat /sys/block/vda/serial)" = NIAOS_TEST_DISK
d-i partman-auto/disk string /dev/vda
d-i partman-auto/method string regular
d-i partman-auto/choose_recipe select atomic
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true
d-i apt-setup/use_mirror boolean false
d-i apt-setup/cdrom/set-first boolean false
popularity-contest popularity-contest/participate boolean false
d-i grub-installer/only_debian boolean true
d-i grub-installer/bootdev string /dev/vda
d-i grub-installer/force-efi-extra-removable boolean true
d-i preseed/late_command string cp /nia-test-grub.cfg /target/etc/default/grub.d/99-niaos-vm-test.cfg; in-target update-grub; echo NIAOS_INSTALL_FINISHED >/dev/ttyS0
d-i finish-install/reboot_in_progress note
d-i debian-installer/exit/poweroff boolean true
