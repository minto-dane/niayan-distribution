#!/bin/sh
set -eu
case "$1" in
    remove|abort-install|disappear)
        dpkg-divert --package niaos-release --remove --rename \
            --divert /usr/lib/os-release.debian /usr/lib/os-release
        ;;
esac
#DEBHELPER#
