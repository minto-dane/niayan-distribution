#!/bin/sh
set -eu
case "$1" in
    install|upgrade)
        dpkg-divert --package niaos-release --add --rename \
            --divert /usr/lib/os-release.debian /usr/lib/os-release
        ;;
esac
#DEBHELPER#
