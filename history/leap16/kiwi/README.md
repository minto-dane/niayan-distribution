# Generated KIWI descriptions

Run the offline `distroctl.py render-kiwi` verifier/renderer with independently
approved input digests and public authorities. No installable sample image,
unverified repositories, private credentials or "default trusted" keys are shipped.
A generated descriptor still requires exact KIWI schema and real-image qualification.
