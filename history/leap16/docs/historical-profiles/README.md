# Historical profile, not an active target

`sles-16.0.json` is retained solely to document the superseded dual-base decision.
Current active input validation accepts only openSUSE Leap 16.0. Moving or signing
this archived file does not make it an accepted target. No SLES subscription is
required by the active build profile. See ADR-0035.
